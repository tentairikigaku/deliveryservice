import 'package:flutter/material.dart';

const exLargeText = TextStyle(
  fontSize: 18,
  fontWeight: FontWeight.bold,
);
const largeText = TextStyle(fontSize: 16);
const smallText = TextStyle(fontSize: 12);

TextStyle greyText({double size = 14}) {
  return TextStyle(color: Colors.grey, fontSize: size);
}
